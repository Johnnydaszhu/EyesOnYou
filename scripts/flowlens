#!/usr/bin/env bash
# Thin wrapper so agents can call `./scripts/flowlens` from the repo root.
set -euo pipefail
ROOT="$(cd "$(dirname "$0")/.." && pwd)"
cd "$ROOT"
exec swift run --package-path "$ROOT" flowlens "$@"
