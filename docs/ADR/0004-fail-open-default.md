# ADR-0004：普通模式默认 Fail Open

## 状态

接受。

## 决策

规则损坏、Provider 状态异常或控制面失联时，普通模式优先保持网络可用；使用 last-known-good，仍失败则 allow。高级用户可以显式开启 Lockdown/default-block。

## 原因

桌面网络工具若因升级或崩溃让整机断网，恢复成本极高。可用性失败应可见但默认不破坏系统网络。

## 约束

Fail open 不得悄悄发生。UI、菜单栏和诊断必须显示状态；事件写入不可篡改的本机诊断序列。
