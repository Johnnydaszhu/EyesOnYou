# ADR-0002：不读取载荷，不做 TLS MITM

## 状态

接受。

## 决策

项目仅处理 flow metadata、目的端点、来源签名、计数和代理握手本身。不得解析用户 HTTP/TLS payload，不安装根证书，不绕过证书固定。

## 原因

- 显著降低隐私和安全风险；
- 避免 CPU/内存和兼容性成本；
- 保持产品定位为网络可观测、防火墙和路由，而不是内容审计；
- 代理可作为不透明字节管道实现。

## 后果

无法准确展示 HTTPS path、Header、请求内容；应用自建加密代理只能做有限推断。这是有意的产品边界。
